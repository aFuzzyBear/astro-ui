---
import XElement from './XElement.astro'

---
    <XElement @is="div"
        @resize={(e)=>{
            const{borderBoxSize:borderBox,contentBoxSize:contentBox,contentRect}=e
            //This is a Bug, cant use `<` as it expects a script tag
            //const bug = () => contentRect.width < 500 ? console.log('<') : console.log('>')
            //bug()
            //contentRect.width < 500 ? console.log('<') : console.log('>')
            // But this works
            //contentRect.width > 500 ? console.log('>') : console.log('<')
            contentRect.width !>= 500  ? console.log('> 500') : console.log('<500')

        }}
    >    
        <slot/>
    </XElement>