---
import Layout from '../../../layouts/landingLayout.astro' 
import getDocsRoutes from '../../../scripts/build/getDocRoutes'
import withMetaData from '../../../scripts/build/withMetaData'
import NestedGrid from '../../../components/Grid/NestedGrid.astro'
let dataSet = []
await withMetaData(getDocsRoutes('api')).then(res=>res.map(x=>dataSet.push({directory:x.props.directory, menu:x.props.menu})))
---
<Layout content={dataSet[0]}>

</Layout>
